import TarotCard from "@/types/shared-types";
import { View } from "moti";
import React from "react";

type Props = {
  card: TarotCard;
};

const Card = () => {
  return <View>Bla</View>;
};

export default Card;
